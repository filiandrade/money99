import { NewTransactionContent } from '@/components/transactions/NewTransactionContent';

export const metadata = { title: 'Nova Transação' };

export default function NewTransactionPage() {
  return <NewTransactionContent />;
}
